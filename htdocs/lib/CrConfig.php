<?php

class CrConfig {

    const databaseName = "";
    const databaseHost = "localhost";
    const databaseUser = "";
    const databasePassword = "";

    /**
     * Path to store log files. Leave empty if you do not want to log anything.
     * Important when using: Make sure the path and its files are outside of 
     * your web root directory and can not be accessed via browser.
     */
    const logPath = "";

}
